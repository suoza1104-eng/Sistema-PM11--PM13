const Dashboard={async render(){const d=await API.get('/api/dashboard');document.querySelector('#view').innerHTML=UI.pageHead('Visão Geral','Resumo gerencial do projeto PM11, qualidade do cadastro e distribuição da carteira.')+`<div class="cards"><div class="card kpi good"><div class="lbl">PLANOS ATIVOS</div><div class="val">${d.plans}</div><div class="hint">periodicidades cadastradas</div></div><div class="card kpi good"><div class="lbl">ITENS DE INSPEÇÃO</div><div class="val">${d.items}</div><div class="hint">ordens potenciais</div></div><div class="card kpi"><div class="lbl">CARACTERÍSTICAS</div><div class="val">${d.characteristics}</div><div class="hint">pontos de controle</div></div><div class="card kpi"><div class="lbl">ROTAS</div><div class="val">${d.routes}</div><div class="hint">sequências cadastradas</div></div></div><div class="grid-2"><div class="panel"><div class="panel-head"><h3>Itens por ciclo</h3></div>${this.bars(d.by_cycle)}</div><div class="panel"><div class="panel-head"><h3>Qualidade do cadastro</h3></div><div class="cards" style="grid-template-columns:repeat(3,1fr);margin:0"><div class="card kpi ${d.issues.missing_time?'bad':'good'}"><div class="lbl">Sem tempo</div><div class="val">${d.issues.missing_time}</div></div><div class="card kpi ${d.issues.missing_route?'warn':'good'}"><div class="lbl">Sem rota</div><div class="val">${d.issues.missing_route}</div></div><div class="card kpi ${d.issues.missing_characteristics?'warn':'good'}"><div class="lbl">Sem características</div><div class="val">${d.issues.missing_characteristics}</div></div></div><div class="sep"></div><h3>Métodos mais utilizados</h3>${d.top_methods.map(x=>`<div style="display:flex;justify-content:space-between;padding:6px;border-bottom:1px solid #edf0eb"><span>${UI.esc(x.label)}</span><b>${x.value}</b></div>`).join('')||'<div class="muted">Sem dados.</div>'}</div></div>`},bars(data){if(!data.length)return '<div class="muted">Cadastre Itens para visualizar.</div>';const mx=Math.max(...data.map(x=>x.value));return `<div class="bar-chart">${data.slice(0,14).map(x=>`<div class="bar" style="height:${Math.max(4,x.value/mx*100)}%"><em>${x.value}</em><span>${UI.esc(x.label)}</span></div>`).join('')}</div>`}}

window.Dashboard=Dashboard;
